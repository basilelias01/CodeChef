n = int(raw_input())
lst = []

for i in range(0, n):
        x, y, z = raw_input().split()
        x = x * y
        print  x
        x, z = int(x), int(z)
        z = x % z
        lst.append(z)

for x in lst:
    print x

